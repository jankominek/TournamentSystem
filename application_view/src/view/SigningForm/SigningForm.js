import React from 'react'
import { SigningBox, SigningFormWrapper } from './SigningForm.styled'

export const SigningForm = () => {
  return (
    <SigningFormWrapper>
        <SigningBox>
            
        </SigningBox>
    </SigningFormWrapper>
  )
}
